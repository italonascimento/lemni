{
  "name": "{{project}}",
  "version": "0.0.1",
  "main": "dist/app.js",
  "private": false,
  "scripts": {
    "clean": "rm -rf dist && mkdir dist",
    "build": "npm run clean && tsc"
  },
  "devDependencies": {
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "@lemni/core": "^{{version}}"
  }
}
