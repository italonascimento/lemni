{
  "name": "reuse",
  "version": "0.0.1",
  "description": "A reactive framework based on React",
  "main": "lib/index.js",
  "author": "Denis Ikeda, Ítalo Nascimento",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib && mkdir lib",
    "install": "pip install mkdocs",
    "docs:build": "mkdocs build"
  },
  "devDependencies": {
    "lerna": "^2.8.0",
    "tslint": "^5.9.1",
    "typescript": "^2.7.1"
  }
}
